# 内网穿透（ngrok）使用说明

## 原理

本系统通过 **ngrok** 实现内网穿透，将本地运行的服务暴露到公网，使外网用户可以通过固定地址访问。

- ngrok 在本地和 ngrok 服务器之间建立一个加密隧道
- 外部请求 → ngrok 公网地址 → 隧道 → 你电脑上的 `localhost:5173`
- 不需要公网 IP，不需要改路由器设置

## 前提条件

- 已完成 ngrok 安装和 authtoken 配置（已在本文档编写前完成）
- ngrok 安装在：`C:\Users\Administrator\ngrok\ngrok.exe`

## 启动步骤

### 第一步：启动本地服务

打开 3 个终端窗口，分别执行以下命令：

**终端 1 — RAG 服务（端口 8000）：**
```powershell
cd C:\Users\Administrator\Desktop\qa-system\rag-service-node
uv run uvicorn app.main:app --host 0.0.0.0 --port 8000
```

**终端 2 — 后端网关（端口 8081）：**
```powershell
cd C:\Users\Administrator\Desktop\qa-system\backend-spring
mvn spring-boot:run -q
```

**终端 3 — 前端界面（端口 5173）：**
```powershell
cd C:\Users\Administrator\Desktop\qa-system\web-frontend
pnpm dev
```

确认三个服务都启动成功后进行下一步。

### 第二步：启动 ngrok 隧道

再开一个终端，执行：

```powershell
& "$env:USERPROFILE\ngrok\ngrok.exe" http 5173
```

启动后会看到类似输出：

```
Forwarding    https://xxxx-xxx-xxx-xxx-xx.ngrok-free.app -> http://localhost:5173
```

`https://xxxx-xxx-xxx-xxx-xx.ngrok-free.app` 就是当前的公网访问地址。

### 第三步：分享地址

把 ngrok 输出的 `https://xxxx-xxx-xxx-xxx-xx.ngrok-free.app` 发给需要访问的人即可。

## 查看当前公网地址

ngrok 运行时，浏览器打开 `http://127.0.0.1:4040` 可查看当前隧道状态和公网地址。

## 停止

在 ngrok 运行的终端按 `Ctrl+C` 即可停止隧道。

## 注意事项

| 问题 | 说明 |
|------|------|
| **地址会变** | ngrok 免费版每次重启会分配新的随机地址，如需固定域名需付费升级 |
| **警告页** | 免费版首次访问会显示 ngrok 确认页，访客点击 "Visit Site" 即可，每人仅一次 |
| **PC 关机** | 关机后隧道断开，下次开机需重新执行上述步骤 |
| **地址获取** | 每次重新启动 ngrok 后，用 `http://127.0.0.1:4040` 查看当前地址 |
