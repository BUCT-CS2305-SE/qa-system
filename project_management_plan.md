# 问答子系统（qa-system）项目管理计划

> 目标：在“知识图谱（Neo4j）+（可选）文档检索 + 大语言模型”的 RAG 架构下，交付一个可演示、可溯源、可扩展的文物知识问答系统；优先保障事实准确性与工程可联调性。

## 1. 范围与目标

### 1.1 交付范围（必须）

- ✅ **简单问答**：至少覆盖 10 类（收藏地/年代/材质/类型/介绍/书画作者/作者生平/同作者作品/同朝代文物/尺寸规格/相似推荐）。
- ✅ **无数据兜底**：知识图谱/检索证据不足时必须返回“暂无相关数据”，禁止编造。
- ✅ **答案溯源**：每条回答必须提供来源说明（`source_name` + `detail_url/source_url`）。
- ✅ **统一接口**：对外统一 `POST /api/qa/ask`，并提供健康检查与反馈接口。
- ✅ **可演示 Web 前端**：包含问答输入、对话展示、来源链接展示。

### 1.2 选做范围（按优先级逐步实现）

- 多轮对话（代词指代、保留最近 5 轮上下文、识别换话题）
- 复杂问答（多跳推理、比较类、统计类、路径查询）
- 反馈机制（有帮助/不准确、问题日志与统计）
- 闲聊与文化导览（限定领域，超域礼貌拒答）

### 1.3 不在范围（本阶段不承诺）

- 大规模生产级运维（高可用、多地域灾备）
- 全量数据清洗/标注体系（只做最小可跑通样例）

## 2. 里程碑与阶段计划（S0~S4）

> 说明：每个阶段都要求“可运行、可演示、可回归”，不接受只写文档不产出可执行结果。

### S0 对齐准备（0.5~1 周）

**目标**：统一接口契约、目录结构与分支协作；确定数据字段与测试样例。

**交付物**：
- `specs/`：`/api/qa/ask` 请求/响应字段（含 `answer/sources/facts/no_data`、错误码）
- `data/samples/questions.json`：最小测试题集（≥ 30 条，覆盖 10 类问答）
- 分支流程明确：`feature/* -> env -> main`，env 仅通过 PR 合并

**验收标准**：
- 任意人可按 README 启动服务并访问 health

### S1 最小闭环（1~2 周）

**目标**：跑通“问 -> 检索 -> 答 -> 溯源”的最小链路，支持 3~5 类问答。

**交付物**：
- `rag-service-node`：FastAPI 原型链路可用（mock 或最小 KG）
- `web-frontend`：最简问答页面（可调用 `/api/qa/ask`），展示 sources
- 日志：记录 requestId、意图、实体、命中/未命中、耗时

**验收标准**：
- 现场演示：输入问题 → 输出答案 + 来源链接；无数据返回“暂无相关数据”

### S2 覆盖补齐（1~2 周）

**目标**：简单问答覆盖到 ≥10 类；接入真实 Neo4j 或至少完成可替换的真实查询层。

**交付物**：
- 意图规则/模板：每类问答有对应的意图标签与 Cypher 模板
- 实体别名归一：同义词/别名映射（可配置 JSON）
- 基础测试：单元测试 + 题集回归（通过率与失败原因统计）

**验收标准**：
- 题集覆盖 10 类问答，回归结果可复现

### S3 联调与质量（1~2 周）

**目标**：前后端联调稳定；溯源展示完善；引入反馈与问题日志（至少记录）。

**交付物**：
- `/api/qa/feedback` 可用（有帮助/不准确）
- sources 展示规范（来源名 + 可点击详情页）
- 观测：基础指标（QPS、延迟、错误率/无数据率）

**验收标准**：
- 演示流程稳定，错误可定位（有日志与 requestId）

### S4 增强（选做，持续）

**目标**：引入文档 RAG 与 LLM 生成（可用 LangChain 进行链路工程化）；多轮/复杂问答等增强能力。

**交付物**（按优先级）：
- 文档向量化与召回（pgvector/Milvus 任一）
- LangChain 检索链与结构化输出解析
- 多轮上下文（最近 5 轮）
- 复杂问答（多跳/统计/比较）

**验收标准**：
- 与 S1~S3 不冲突，新增能力可开关、可回归

## 3. 工作分解结构（WBS）

### 3.1 产品与交互
- 需求归档：问题类型、输出格式、溯源样式
- 前端页面：输入区/对话区/来源区/反馈区（选做）

### 3.2 接口与规范
- `POST /api/qa/ask`：请求/响应字段、错误码、示例
- `POST /api/qa/feedback`：反馈模型与落库/日志

### 3.3 RAG 服务（rag-service-node）
- 输入理解：标准化、意图识别、实体抽取、别名归一
- 查询构建：意图 → Cypher 模板
- KG 检索：mock → Neo4j
- 答案组织：规则模板 →（可选）LLM 生成
- 溯源组装：source_name/source_url/detail_url
- LangChain（可选）：检索链、prompt、输出解析、工具调用（受控）

### 3.4 工程与质量
- 单测/题集回归
- 日志与可观测性
- CI（可选）：lint/test

## 4. 角色与职责（RACI 简版）

> 可按组内实际人名替换。

- 组长/集成负责人（A）：把控 env 合并与发布口径、验收与演示
- 后端/接口负责人（R）：接口契约、错误码、鉴权/限流、联调
- RAG 负责人（R）：输入理解、查询构建、KG 检索、LangChain/LLM 接入
- 前端负责人（R）：问答页面、sources/feedback 展示、代理与联调
- 测试/数据负责人（R）：题集构建、回归脚本、覆盖统计

## 5. 风险清单与应对

- **数据不足/图谱字段缺失** → 先保证“无数据兜底 + 可解释”，并记录缺口清单
- **LLM 幻觉风险** → facts 与生成内容区分；无证据一律 no_data；输出结构化校验
- **接口频繁变更导致联调返工** → S0 锁定契约；变更必须版本化
- **多轮/复杂问答难度高** → 作为 S4 选做，不影响 S1~S3 验收
- **技术栈分歧（Python vs Node）** → 先以 FastAPI 原型稳定接口与流程，再评估迁移价值

## 6. 进度跟踪与会议机制

- 每日站会（10 分钟）：昨天产出/今天计划/阻塞点
- 每周里程碑评审（30~45 分钟）：按阶段验收标准过一遍
- PR 规则：所有代码必须 PR → env，至少 1 人 review

## 7. 验收清单（Checklists）

- [ ] `/api/qa/ask` 返回结构稳定，前端可直接渲染
- [ ] 10 类简单问答覆盖完成
- [ ] 无数据兜底生效（抽测 10 条无答案问题）
- [ ] sources 可点击且指向详情页
- [ ] 题集回归可运行并生成结果摘要
- [ ] 演示脚本（问题列表 + 预期输出 + 失败兜底话术）
