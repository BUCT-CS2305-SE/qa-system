# 问答子系统（qa-system）项目管理计划

版本：v1.2（结项） | 更新日期：2026-06-10

> 目标：在"知识图谱（Neo4j）+ （可选）文档检索 + 大语言模型"的 RAG 架构下，交付一个可演示、可溯源、可扩展的文物知识问答系统；优先保障事实准确性与工程可联调性。
>
> **项目状态：已完成交付。** 开发周期：第 9 周 ~ 第 15 周（2026-05-04 ~ 2026-06-15），共 7 周。

---

## 1. 范围与目标

### 1.1 交付范围（全部完成）

| 交付项 | 计划 | 实际 | 状态 |
|--------|------|------|------|
| 简单问答 | ≥10 类 | **12 类**（收藏地/年代/材质/类型/介绍/书画作者/作者生平/同作者作品/同朝代文物/尺寸规格/相似推荐/馆藏量） | ✅ |
| 无数据兜底 | 必须 | 已实现，hybrid + remote 双模式验证通过 | ✅ |
| 答案溯源 | 必须 | 每条回答含 `source_name` + `detail_url`，前端可点击跳转 | ✅ |
| 统一接口 | `POST /api/qa/ask` | 已实现，额外提供 `/feedback`、`/health`、`/history/list`、`/summary` | ✅ |
| Web 前端 | 可演示 | React 19 + Vite 8，Chat UI + 侧边栏 + 来源展示 + 反馈按钮 + 深色模式 | ✅ |

### 1.2 选做范围（全部完成）

| 交付项 | 计划 | 实际 | 状态 |
|--------|------|------|------|
| 多轮对话 | 选做 | 已完成：代词指代消解 + 话题切换检测 + 30min 上下文超时 + 前后端双存储 | ✅ |
| 复杂问答 | 选做 | **4 类已完成**：多跳推理/文物对比/朝代统计/流转路径 | ✅ |
| 反馈机制 | 选做 | 已完成：👍/👎 前端按钮 → 后端落库 → RAG 日志 → 统计摘要 | ✅ |
| API 鉴权与限流 | 选做 | 已完成：双重鉴权（X-Api-Key + JWT）+ IP 滑动窗口 60s/60 次 | ✅ |
| Docker 部署 | 选做 | 已完成：三服务 Dockerfile + docker-compose.yml + 一键启停脚本 | ✅ |

### 1.3 不在范围（明确推迟）

| 项目 | 说明 |
|------|------|
| 文档 RAG（向量检索） | 推迟至下一阶段，当前仅 KG 单路检索 |
| LangChain 检索链集成 | 推迟至下一阶段，当前为手写 RAG 管道 |
| 大规模生产级运维 | 不在课程范围内 |
| 全量数据清洗/标注体系 | 不在课程范围内 |

---

## 2. 里程碑与阶段计划（S0~S4）—— 实际执行记录

### S0 对齐准备（第 9 周，2026-05-04 ~ 05-10）✅

**目标**：统一接口契约、目录结构与分支协作；确定数据字段与测试样例。

**交付物**：
- ✅ `specs/api-contract.md`：`/api/qa/ask` 请求/响应字段、错误码表（2000~5004）、鉴权/限流规则
- ✅ `data/samples/questions.json`：初始测试题集
- ✅ 分支流程明确：`feature/* -> env -> main`
- ✅ 三服务目录结构确立：`web-frontend/`、`backend-spring/`、`rag-service-node/`

**实际耗时**：1 周

---

### S1 最小闭环（第 10 周，2026-05-11 ~ 05-17）✅

**目标**：跑通"问 → 检索 → 答 → 溯源"的最小链路。

**交付物**：
- ✅ FastAPI 原型链路（mock 模式），支持 3 类问答
- ✅ React + TypeScript 前端页面搭建
- ✅ Spring Boot 网关搭建
- ✅ 问答统一返回结构设计（`status/answer/sources/facts/no_data`）
- ✅ 日志记录：requestId、意图、实体、命中/未命中、耗时

**实际耗时**：1 周

---

### S2 覆盖补齐（第 11~12 周，2026-05-18 ~ 05-31）✅

**目标**：简单问答覆盖到 ≥10 类；接入真实 KG API。

**交付物**：
- ✅ 12 类简单问答意图规则 + 12 个 Cypher 模板（`templates.py`）
- ✅ 意图分类器（关键词打分）+ 实体别名匹配（22 条，`entity_aliases.json`）
- ✅ mock 数据覆盖全部 12 类意图
- ✅ 前端 Chat UI 完整搭建
- ✅ 前端接入 `POST /api/qa/ask`（含超时控制、API Key 鉴权）
- ✅ Spring Boot 网关完善（转发/重试/fallback/鉴权/限流）
- ✅ 会话持久化：前端 localStorage（5 天 TTL）+ 后端 H2（30 天保留）
- ✅ 4 类复杂问答意图 + 模板 + mock
- ✅ 复杂问答 4 类意图接入数据组真实 API
- ✅ 多轮对话增强（代词消解 + 话题切换 + 30min 超时）
- ✅ 单元测试扩充至 19 个，测试题集 33 条

**实际耗时**：2 周

---

### S3 联调与质量（第 13~14 周，2026-06-01 ~ 06-07）✅

**目标**：前后端联调稳定；溯源展示完善；工程化配套。

**交付物**：
- ✅ `/api/qa/feedback` 前后端打通（落库 + 转发 RAG）
- ✅ sources 展示规范（来源名 + 可点击详情页），前端完整渲染
- ✅ Micrometer 监控指标（QPS/延迟/错误率计数器）
- ✅ Docker Compose 三服务编排（健康检查 + 依赖顺序）
- ✅ `start-dev.ps1` / `stop-dev.ps1` / `docker-deploy.ps1` / `run-tests.ps1` 四个脚本
- ✅ API 契约文档（`specs/api-contract.md`）
- ✅ 测试报告 v1.2（55 自动化用例 + 10 真实 KG 用例，100% 通过）

**实际耗时**：1 周

---

### S4 增强 & 收尾（第 15 周，2026-06-08 ~ 06-10）✅

**目标**：Bug 修复、生产加固、文档终版。

**交付物**：
- ✅ 修复 13 个 Bug（含 no_data 兜底失效、限流未生效、鉴权缺失、意图分类误判、实体推理错误等）
- ✅ 全链路 JWT 鉴权贯通（前端 → 后端 → RAG → KG API）
- ✅ 局域网跨域访问支持
- ✅ 中/英文自适应搜索
- ✅ 用户使用手册 v1.2
- ✅ 项目完成度总结（`COMPLETION_SUMMARY.md`）
- ✅ 模块与分工报告（`MODULE_REPORT.md`）
- ✅ 会议纪要补全（第 11~15 周）
- ✅ 管理计划、设计报告、需求规格说明书终版（v1.2）

**已明确推迟**：文档 RAG（向量检索）、LangChain 检索链集成

---

## 3. 工作分解结构（WBS）—— 实际交付对照

### 3.1 产品与交互（完成）

| 子项 | 文件/模块 | 状态 |
|------|-----------|------|
| 问答对话界面 | `ChatBox.tsx`（用户/助手气泡、打字动画、来源展示、反馈按钮） | ✅ |
| 问题输入 | `ChatComposer.tsx`（Enter 发送、Shift+Enter 换行） | ✅ |
| 侧边栏 | `SideHistory.tsx`（会话列表、新建/删除/切换、连接状态） | ✅ |
| 会话管理 | `useChat.ts`（localStorage 持久化、5 天 TTL、GC、500 条上限） | ✅ |
| 样式系统 | `app.css`（23 个 CSS 变量、暗色/亮色、响应式） | ✅ |

### 3.2 接口与规范（完成）

| 子项 | 说明 | 状态 |
|------|------|------|
| `POST /api/qa/ask` | Spring Boot 网关转发 → RAG 服务，统一响应结构 | ✅ |
| `POST /api/qa/feedback` | 落库 + 转发 RAG，支持 helpful/comment | ✅ |
| `GET /api/qa/history/list` | H2 持久化，按会话分页查询 | ✅ |
| `GET /api/qa/health` | 健康检查，免鉴权 | ✅ |
| `GET /api/qa/summary` | RAG 统计摘要（意图分布/状态分布/失败清单） | ✅ |
| API 契约文档 | `specs/api-contract.md`（4 接口 + schema + 错误码 + 鉴权/限流） | ✅ |

### 3.3 RAG 服务（rag-service-node，完成）

| 子模块 | 功能 | 状态 |
|--------|------|------|
| 输入理解 | 规范化 → 实体抽取（4 类/22 别名）→ 意图分类（16 类）→ 上下文解析 | ✅ |
| 查询构建 | 意图 → 16 个模板映射 → 参数填充 | ✅ |
| KG 检索 | 8 个数据组 API 端点对接 + mock 降级（hybrid 模式） | ✅ |
| 答案生成 | 16 类规则模板 + LLM 润色（auto 模式） | ✅ |
| 溯源组装 | sources + facts 去重合并，`detail_url` 可点击 | ✅ |
| 反馈记录 | 内存日志 + 统计汇总 | ✅ |
| 单元测试 | 19 个 pytest 用例（覆盖全部 16 意图 + 多轮 + 反馈） | ✅ |

### 3.4 工程与质量（完成）

| 子项 | 说明 | 状态 |
|------|------|------|
| 回归题集 | `questions.json`（33 条，覆盖 16 类意图） | ✅ |
| 单元测试 | 19 个 pytest（rag） + 16 个 JUnit（backend） | ✅ |
| 集成测试 | 33 个真实 KG + LLM 用例 | ✅ |
| Docker 三服务 | `docker-compose.yml`（依赖顺序 + 健康检查） | ✅ |
| 一键脚本 | `start-dev.ps1` / `stop-dev.ps1` / `docker-deploy.ps1` / `run-tests.ps1` | ✅ |
| 日志与可观测性 | request_id 全链路 + Micrometer 指标 | ✅ |

---

## 4. 角色与职责

| 角色 | 职责 | 负责人 |
|------|------|--------|
| 组长/集成 | 整体把控、env 合并、验收演示、文档汇总 | 张恒卓 |
| 前端 | web-frontend 全部（Chat UI、会话管理、API 客户端、样式） | 前端组 |
| 后端 | backend-spring 全部（网关、鉴权、限流、历史、反馈） | 后端组 |
| RAG 核心 | rag-service-node 全部（输入理解、查询构建、KG 检索、答案生成） | 算法组 |
| 工程化/测试 | Docker、脚本、题集、测试、API 契约 | 全体 |

---

## 5. 风险清单与应对（回顾）

| 风险项 | 是否发生 | 实际应对 |
|--------|----------|----------|
| 数据不足/图谱字段缺失 | ✅ 发生 | 先保证 no_data 兜底 + 空字段自动拼装可用字段；记录缺口清单 |
| LLM 幻觉风险 | ✅ 发生 | facts 与生成内容区分；无证据一律 no_data；LLM 默认关闭 mock 模式 |
| 接口频繁变更导致联调返工 | ✅ 发生 | S0 锁定契约，后续变更记录在 `specs/` |
| 多轮/复杂问答难度高 | 未成风险 | 第 11~12 周提前启动并完成，比预期顺利 |
| KG API 不稳定/鉴权变更 | ✅ 发生 | hybrid 降级模式 + JWT 鉴权全链路适配（Bug B-008） |
| 实体别名与 KG 数据不匹配 | ✅ 发生 | 更新为真实 KG 可搜索实体名，增加关键词回退策略 |

---

## 6. 进度跟踪与会议机制（实际执行）

- 周例会：每周一 21:00 腾讯会议，共 7 次（第 9~15 周）
- 会议纪要：`qa-system.wiki/` 目录下完整归档
- 协作方式：GitHub Feature Branch → PR → env → main
- 总计 PR 数：约 18 个

---

## 7. 验收清单（全部通过）

- [x] `/api/qa/ask` 返回结构稳定，前端可直接渲染
- [x] **12 类**简单问答覆盖完成（超出计划 2 类）
- [x] no_data 兜底生效（55 用例验证，hybrid + real KG 双重通过）
- [x] sources 可点击且指向详情页（artic.edu 等博物馆链接）
- [x] 题集回归可运行并生成结果摘要（`run-tests.ps1`，55 用例 100%）
- [x] 演示脚本（5 场景：简单问答 → 多轮 → 复杂问答 → no_data → 反馈）
- [x] **4 类**复杂问答（多跳推理/文物对比/朝代统计/流转路径）
- [x] 多轮对话（代词指代 + 话题切换，7 步全部通过）
- [x] 双重鉴权（X-Api-Key + JWT）生效
- [x] IP 限流（60s/60 次，并发测试触发 429）
- [x] Docker 三服务一键部署
- [x] 中/英文自适应搜索

---

## 8. 项目总结

| 维度 | 数据 |
|------|------|
| 开发周期 | 第 9 周 ~ 第 15 周（7 周） |
| 代码文件 | ~50 个（Java + Python + TypeScript） |
| 意图覆盖 | 16 类（12 简单 + 4 复杂） |
| 对接 KG API | 8 个端点 |
| 单元测试 | 55 个（19 pytest + 36 回归题集） |
| 集成测试 | 33 个（真实 KG + LLM） |
| 修复 Bug | 15 个 |
| 文档交付 | 9 份（SRS/设计/管理/模块/完成度/测试/用户手册/API 契约/会议纪要×7） |
