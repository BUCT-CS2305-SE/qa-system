# 固定域名 + 免费内网穿透 操作指南

总成本：**6.5 元/年**（`.xyz` 域名第一年），后续每年续费约 70 元，到期换一个就行。

---

## 一、买域名（5 分钟）

1. 打开 **https://spaceship.com**
2. 注册账号（邮箱即可）
3. 搜索一个你喜欢的 `.xyz` 域名，比如 `qa-culturerelic.xyz`
4. 加入购物车 → 结账 → 支持支付宝/微信 → **约 6.5 元**
5. 付款完成

---

## 二、注册 Cloudflare（2 分钟）

1. 打开 **https://cloudflare.com**
2. 注册账号（免费，秒过）
3. 登录后点 **"添加站点"**
4. 输入你刚买的域名（如 `qa-culturerelic.xyz`）
5. 选 Free 计划 → 继续

---

## 三、把域名接入 Cloudflare

1. Cloudflare 会给你两个 **nameserver 地址**（如 `elisa.ns.cloudflare.com`）
2. 回到 Spaceship → 我的域名 → 管理 → 找到 "Nameservers" 或 "域名服务器"
3. 把默认的改成 Cloudflare 给的两个地址 → 保存
4. 等待生效（通常 2-10 分钟）

---

## 四、创建 Cloudflare Tunnel

1. 在 Cloudflare 左侧菜单 → **Zero Trust** → 进入
2. 左侧 → **Networks** → **Tunnels**
3. 点 **"Create a tunnel"**
4. 起个名字（如 `qa-system`）→ **Save tunnel**
5. 在 "Choose your environment" 页面：
   - 选 **Windows**
   - 复制下面的安装命令（类似 `winget install Cloudflare.cloudflared`）
   - 先跳过去，后面再说

---

## 五、配置隧道指向本地服务

1. 在 Tunnel 详情页 → **Public Hostname** → **Add a public hostname**
2. 填写：
   - **Subdomain**：不填（留空，直接用根域名）
   - **Domain**：选你的域名
   - **Type**：`HTTP`
   - **URL**：`localhost:5173`
3. 点 **Save hostname**

---

## 六、在你电脑上运行隧道

1. 如果还没装 cloudflared：
   ```powershell
   winget install Cloudflare.cloudflared
   ```
2. 启动隧道（把 `<token>` 换成你在第四步看到的 token）：
   ```powershell
   cloudflared tunnel run --token <token>
   ```
3. 看到 `Registered tunnel connection` 就成功了

---

## 七、完成

浏览器打开 `https://你的域名.xyz`，就能访问你的 QA 系统了。

以后每次开机，先启动 QA 的三个服务，再运行 `cloudflared tunnel run --token <token>`，域名永远不变。

---

## 补充

- 如果 Cloudflare Tunnel 在校园网连不上，可能需要用其他方式。届时根据报错信息调整。
- `.xyz` 域名续费较贵，第二年可以换个新域名重新按第一年价格买。
