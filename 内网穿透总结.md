# 固定域名内网穿透 — 完成总结

## 最终效果

**`https://qa-culturerelic.xyz`** — 永久固定域名，外网直接访问你电脑上的 QA 系统。

## 做了什么

| 步骤 | 内容 |
|------|------|
| 1 | 在 **Spaceship** 花 6.5 元买了 `qa-culturerelic.xyz` 域名 |
| 2 | 在 **Cloudflare**（免费）添加站点，接管 DNS |
| 3 | 把域名的 nameserver 从 Spaceship 切到 Cloudflare（`melina.ns.cloudflare.com` / `vick.ns.cloudflare.com`） |
| 4 | 在 Cloudflare Zero Trust 创建了 Tunnel，配置 `qa-culturerelic.xyz` → `localhost:5173` |
| 5 | Vite 配置中加入 `qa-culturerelic.xyz` 白名单 |

## 架构

```
外网用户 → https://qa-culturerelic.xyz
                ↓
         Cloudflare 边缘节点（SSL 加密）
                ↓
         cloudflared 隧道（你电脑后台运行）
                ↓
         Vite 开发服务器 :5173
                ↓
         Spring Boot :8081 → Python RAG :8000 → 知识图谱 API
```

## 每次开机启动步骤

打开 **4 个终端窗口**，依次运行：

**终端 1 — RAG 服务（先启）：**
```powershell
cd C:\Users\Administrator\Desktop\qa-system\rag-service-node
uv run uvicorn app.main:app --host 0.0.0.0 --port 8000
```

**终端 2 — 后端网关：**
```powershell
cd C:\Users\Administrator\Desktop\qa-system\backend-spring
mvn spring-boot:run -q
```

**终端 3 — 前端界面：**
```powershell
cd C:\Users\Administrator\Desktop\qa-system\web-frontend
pnpm dev
```

**终端 4 — Cloudflare 隧道：**
```powershell
C:\Users\Administrator\AppData\Local\Microsoft\WinGet\Packages\Cloudflare.cloudflared_Microsoft.Winget.Source_8wekyb3d8bbwe\cloudflared.exe tunnel run --token eyJhIjoiYzljYmVhODQxNTAyYmNhMTkzMjc1MDFmZTVlOTU5ZDAiLCJ0IjoiMjAxZDU3NmYtZDBiMS00ZDc4LWFiNjgtNDY0NjVmM2RlMzUxIiwicyI6IlpXWXlPV1F5TmpjdFlURTVNQzAwTnpCaExXSXhNMll0Wmpaa01qaGxOREl3WlRFeSJ9
```

四个终端都显示正常运行后，**`https://qa-culturerelic.xyz` 即可访问**，不需要任何额外操作。

## 停止

每个终端窗口按 `Ctrl+C` 即可停止对应服务。
