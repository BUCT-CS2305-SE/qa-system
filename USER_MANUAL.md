# 文物知识问答子系统 — 用户使用手册

版本：v1.2 | 更新日期：2026-06-08

---

## 一、系统简介

本系统是一个面向**中国海外流失文物**的智能问答平台。输入自然语言问题，系统从知识图谱（Neo4j）中检索事实，可选通过大语言模型（DeepSeek）润色生成自然语言回答，并标注信息来源。

**核心特点：**
- 支持 **16 类问题**（收藏地、年代、材质、作者、对比、统计等）
- 每一条答案都有**来源链接**，可点击验证
- 查不到时会明确说"暂无相关数据"，**不会瞎编**
- 支持**连续对话**，记住上下文（30 分钟超时）
- 支持**大模型润色**（auto 模式），让回答更自然流畅
- **多层安全防护**：API 鉴权 + IP 限流（60次/60s）

---

## 二、快速开始

### 2.1 访问地址

| 环境 | 地址 |
|------|------|
| 本地开发 | `http://localhost:5173` |
| 局域网 | `http://你的IP:5173`（见下方 2.1.1） |
| Docker 部署 | `http://localhost:5173` |

#### 2.1.1 局域网访问（开发联调）

为了让 Web 端能跳转到本系统，前端已绑定 `0.0.0.0`。获取本机 IP：

```powershell
ipconfig | findstr "IPv4"
```

Web 端通过 `http://<你的IP>:5173` 访问即可。

#### 2.1.2 Token 鉴权（必读）

系统要求**双重认证**：`X-Api-Key` + `Authorization` token。前端代码已内置 `X-Api-Key=qa-demo-key`，但 `Authorization` token 需由 Web 端用户登录后通过跳转携带，或开发者手动注入。

**开发测试时**（无 Web 端跳转），打开页面后按 F12 → Console，粘贴以下代码回车：

```js
localStorage.setItem('auth_token', 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..KuFPfUrMU10PgmQapIOquLarWwJ93gHCP9aI7dqOcmY')
```

然后 **F5 刷新页面**。token 会持久化到 localStorage，之后每次打开都能直接使用。如需切换 token（如从管理员换到普通用户），再次执行上述代码即可覆盖。

> **注意**：token 有过期时间，若收到 401 请联系数据组获取新 token 并重新执行上述步骤。

系统依赖三个服务，启动后自动连接：

| 服务 | 端口 | 作用 |
|------|------|------|
| Web 前端 | 5173 | 用户交互界面 |
| Spring Boot 网关 | 8081 | 鉴权、限流、转发 |
| RAG 服务（Python） | 8000 | 意图识别、KG检索、答案生成 |

### 2.2 界面一览

```
┌──────────────────────────────────────────────────────────────┐
│ ┌──────────────┐ ┌─────────────────────────────────────────┐ │
│ │  文物知识问答  │ │  文物知识问答          [Neo4j] [RAG]     │ │
│ │   KG+RAG     │ ├─────────────────────────────────────────┤ │
│ │              │ │                                         │ │
│ │ [新建对话]    │ │  👤 用户                                 │ │
│ │              │ │  女史箴图在哪个博物馆？                    │ │
│ │ ─ 对话1      │ │                                         │ │
│ │ ─ 对话2      │ │  🤖 助手                                │ │
│ │ ─ 对话3      │ │  女史箴图现藏于大英博物馆。               │ │
│ │              │ │  📎 来源：大英博物馆 [链接]               │ │
│ │              │ │  👍 有帮助  👎 不准确                    │ │
│ │              │ │                                         │ │
│ │              │ ├─────────────────────────────────────────┤ │
│ │              │ │ 💬 输入问题...                  [发送]   │ │
│ │ [反馈]       │ │ Enter发送 / Shift+Enter换行             │ │
│ │ ● 已连接     │ └─────────────────────────────────────────┘ │
│ └──────────────┘                                           │
└──────────────────────────────────────────────────────────────┘
```

| 区域 | 说明 |
|------|------|
| **左侧边栏** | 新建对话、切换历史对话、删除对话、提交使用反馈 |
| **顶栏** | 系统标题和描述，技术栈标识（Neo4j / RAG） |
| **中间对话区** | 一问一答，含来源链接、事实卡片、LLM润色标注 |
| **底部输入区** | 输入问题，Enter 发送，Shift+Enter 换行 |

---

## 三、提问指南

### 3.1 支持的问题类型

#### 基础查询（12 类）

| 类型 | 示例问题 |
|------|----------|
| 收藏地 | "女史箴图在哪个博物馆？" / "清明上河图的收藏地是哪里？" |
| 年代 | "青铜奔马属于哪个朝代？" / "马踏飞燕是什么时期的？" |
| 材质 | "马踏飞燕是什么材质的？" / "Tea Bowl and Dish的材质是什么？" |
| 类型 | "Tea Bowl and Dish属于什么类型？" |
| 介绍 | "请介绍一下清明上河图" / "介绍一下青铜奔马" |
| 尺寸 | "女史箴图的尺寸是多少？" / "清明上河图的规格是多少？" |
| 作者 | "清明上河图的作者是谁？" / "马踏飞燕的作者是谁？" |
| 作者生平 | "顾恺之的生平经历是怎样的？" / "张择端的生平是怎样的？" |
| 同作者作品 | "张择端还有哪些作品？" / "顾恺之还有哪些作品？" |
| 同朝代文物 | "唐代有哪些代表性文物？" / "宋代有什么代表文物？" |
| 相关推荐 | "推荐一些和女史箴图类似的文物" |
| 馆藏量 | "大都会博物馆共收藏了多少件中国文物？" |

#### 高级查询（4 类）

| 类型 | 示例问题 |
|------|----------|
| 文物对比 | "比较女史箴图和清明上河图" |
| 年代统计 | "唐代文物统计" |
| 流转路径 | "女史箴图的流转路径" |
| 多跳推理 | "Admonitions Scroll经过哪些地方？" |

### 3.2 回答模式

系统支持两种回答模式：

| 模式 | 触发方式 | 特点 |
|------|----------|------|
| **rule**（规则模板） | 自动识别匹配的问题类型 | 速度快（~0.5s），基于结构化模板直接生成 |
| **auto**（大模型润色） | 其他类型的问题 | 调用 DeepSeek 大模型，回答更自然（~3-4s） |

前端会根据问题类型自动选择合适的模式。简单查询（收藏地、材质等）优先使用 rule 模式；复杂问题使用 auto 模式。

### 3.3 提问技巧

- **尽量包含文物全名**，如"女史箴图"而非"那个画"
- **可以中英文混用**，系统支持双语实体识别（"Admonitions Scroll" = "女史箴图"）
- **不知道文物名也可以试试**，系统会尝试从问题中推断
- **可以追问**，系统会记住上一轮的文物（见"连续对话"章节）

---

## 四、理解答案

### 4.1 答案结构

每一条回答包含以下信息：

```
🤖 助手
女史箴图现藏于大英博物馆。        ← 答案正文

📎 来源：大英博物馆                ← 数据出处
🔗 https://www.britishmuseum...   ← 可点击的详情链接

📋 事实：                         ← 结构化事实（KG 检索结果）
  · 收藏地：大英博物馆
  · 年代：东晋

💡 本回答由 deepseek-chat 基于知识图谱事实生成  ← LLM 润色标注（auto 模式）
```

### 4.2 答案标识

| 标识 | 含义 |
|------|------|
| 📎 来源 | 知识图谱中的数据来源（博物馆名称） |
| 🔗 链接 | 点击可跳转到来源详情页（新窗口打开） |
| 📋 事实 | KG 检索到的结构化事实列表（subject/predicate/object） |
| 💡 LLM 标注 | 回答由大模型基于事实生成，关键事实已核实（仅 auto 模式显示） |
| 规则版标注 | 回答由规则模板直接生成，如需润色可配置 LLM（仅 rule 模式显示） |

### 4.3 no_data 兜底

当知识图谱中没有相关信息时，系统会明确告知：

```
🤖 助手
暂无相关数据        ← 不会编造任何内容

source: []
no_data: true
```

**这是预期行为——系统不会编造不存在的信息。** 如果遇到 no_data，可以：
- 换个已知文物再试（如"女史箴图"、"清明上河图"、"青铜奔马"）
- 尝试中英文不同的写法
- 检查是否有错别字

---

## 五、连续对话（多轮）

### 5.1 代词指代

系统能记住上一轮问的文物，可以用"它"来追问：

```
👤 女史箴图在哪个博物馆？
🤖 女史箴图现藏于大英博物馆。

👤 它的材质是什么？          ← "它"=女史箴图
🤖 女史箴图的材质为绢本。

👤 它的尺寸呢？
🤖 女史箴图的尺寸为24.8cm × 348.2cm。
```

### 5.2 话题切换

想换问题直接问就行，系统会自动检测话题变化：

```
👤 女史箴图在哪个博物馆？
🤖 女史箴图现藏于大英博物馆。

👤 换个话题，清明上河图的作者是谁？
🤖 清明上河图的作者是张择端。    ← 已切换至新文物

👤 它的收藏地呢？               ← "它"=清明上河图
🤖 清明上河图现藏于故宫博物院。
```

也可以直接问新文物，不说"换个话题"也会被检测到。

### 5.3 会话管理

| 操作 | 位置 | 说明 |
|------|------|------|
| 新建对话 | 左侧栏 [+新建对话] | 开启一个空白会话 |
| 切换对话 | 左侧栏点击对话条目 | 回到之前的对话，自动从服务器加载历史 |
| 删除对话 | 鼠标悬停对话条目 → × 按钮 | 删除该对话及所有消息 |
| 自动过期 | 不需要手动操作 | 浏览器端 5 天未使用自动清理；服务器端 30 天 |

---

## 六、反馈

### 6.1 对单条回答反馈

每条助手回答下方有反馈按钮：

```
👍 有帮助    👎 不准确
```

点击后状态会高亮，反馈被记录到服务器。同一对话的反馈状态在切换会话后会保留。

### 6.2 对系统总体反馈

左侧栏底部有"反馈"按钮（💬 图标），点击弹出反馈弹窗，可以填写文字意见和建议。

---

## 七、常见问题

**Q: 我输入了问题但返回"暂无相关数据"？**

A: 这说明该文物暂时不在知识图谱中，或该属性暂无数据。系统不会编造答案。换个已知文物试试，如"女史箴图"、"清明上河图"、"青铜奔马"。

**Q: 可以一次问两个文物吗？**

A: 试试"对比"功能："比较女史箴图和清明上河图"。普通问题建议一次只问一个。

**Q: 系统支持什么语言？**

A: 中文和英文都可以。文物名称支持中英文别名，如"Admonitions Scroll"等同"女史箴图"。

**Q: 回答的来源链接点不开？**

A: 来源链接指向知识图谱数据提供方（`se-cs2305.yazs.top`）的详情页。如果无法访问，可能是网络限制或来源网站暂时不可用，不影响答案准确性。

**Q: 对话历史能存多久？**

A: 浏览器 localStorage 保留 5 天，服务器 H2 数据库保留 30 天。重要查询结果建议及时截图保存。

**Q: 输入框只能输入一行吗？**

A: 输入框支持多行，按 Shift+Enter 换行，Enter 发送。

**Q: 为什么有些回答比较慢？**

A: auto 模式需要调用大模型（DeepSeek），约 3-4 秒；rule 模式（仅 KG）约 2 秒。远程知识图谱 API 的网络延迟是主要因素。

**Q: 频繁发请求会被限制吗？**

A: 系统有 IP 级别限流（60 次/60 秒窗口）。正常使用不会触发；超过阈值会收到"请求过于频繁"提示，稍等即可恢复。

---

## 八、技术参考（开发者）

### 8.1 系统架构

```
浏览器 (:5173) → Spring Boot (:8081) → FastAPI RAG (:8000) → KG API
                 ├ 鉴权 (X-Api-Key + Authorization)
                 ├ 限流 (60次/60s)
                 ├ 历史/反馈 (H2)
                 ├ CORS
                 └ Token 透传 (Authorization → X-Kg-Token)
                                     ├ 意图分类 (16种)
                                     ├ 实体抽取 (33个别名)
                                     ├ 中/英文自适应搜索
                                     ├ KG 检索 (hybrid模式)
                                     └ 答案生成 (rule/auto)
```

### 8.2 环境变量

前端（`web-frontend/.env`）：

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `VITE_API_BASE` | 后端网关地址 | `http://127.0.0.1:8081` |
| `VITE_API_KEY` | API 鉴权密钥 | `qa-demo-key` |
| `VITE_AI_API_URL` | AI 润色接口（可选） | 空（不启用） |
| `VITE_AI_API_KEY` | AI 润色鉴权密钥 | 空 |

RAG 服务（`rag-service-node/.env`）：

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `qa_graph_backend` | KG 后端模式 | `hybrid`（remote/mock/hybrid） |
| `qa_llm_backend` | LLM 后端模式 | `mock`（openai/mock） |
| `qa_llm_api_url` | LLM API 地址 | DeepSeek API |
| `qa_llm_model` | LLM 模型名 | `deepseek-chat` |
| `qa_llm_api_key` | LLM API 密钥 | — |
| `qa_kg_api_key` | KG API JWT 令牌（开发兜底） | — |
| `qa_kg_api_key_header` | KG API 鉴权请求头名 | `Authorization` |
| `qa_kg_api_key_prefix` | KG API 鉴权前缀 | `Bearer ` |

### 8.3 运行测试

```powershell
# 单元测试 + 回归题集（mock 模式，55 用例）
.\scripts\run-tests.ps1

# HTTP 集成测试（真实 KG + LLM，33 用例）
cd rag-service-node
python -m pytest tests/test_integration.py -v
```

---

## 九、快捷参考卡

```
┌─────────────────────────────────────────────────┐
│              文物知识问答 — 速查卡                │
├─────────────────────────────────────────────────┤
│  收藏地  →  "[文物名]在哪个博物馆？"              │
│  年代    →  "[文物名]是什么时期的？"              │
│  材质    →  "[文物名]是什么材质的？"              │
│  类型    →  "[文物名]属于什么类型？"              │
│  介绍    →  "介绍一下[文物名]"                   │
│  尺寸    →  "[文物名]的尺寸是多少？"              │
│  作者    →  "[文物名]的作者是谁？"               │
│  作者生平→  "[作者名]的生平经历是怎样的？"        │
│  同作者  →  "[作者名]还有哪些作品？"              │
│  同朝代  →  "[朝代名]有哪些代表性文物？"          │
│  推荐    →  "有哪些类似[文物名]的文物？"          │
│  馆藏量  →  "[博物馆名]收藏了多少件文物？"        │
│  对比    →  "比较[文物A]和[文物B]"               │
│  统计    →  "[朝代名]文物统计"                    │
│  流转    →  "[文物名]的流转路径"                 │
│  多跳    →  "[文物名]经过哪些地方？"              │
├─────────────────────────────────────────────────┤
│  追问: "它的材质呢？" / "它的尺寸呢？"            │
│  换话题: "换个话题..." / 直接问新问题             │
│  反馈: 每个回答底下 👍 / 👎                      │
│  模式: rule（快速模板）/ auto（LLM润色）          │
│  限流: 60次/60秒，超限自动恢复                    │
└─────────────────────────────────────────────────┘
```
