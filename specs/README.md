# API 数据规格文档

| 文件 | 用途 |
|------|------|
| `api-contract.md` | 完整 API 契约：`POST /api/qa/ask` / `POST /api/qa/feedback` / `GET /api/qa/history/list` / `GET /api/health`，含请求/响应 schema、错误码表（2000~5004）、鉴权与限流规则 |

`api-contract.md` 是前后端联调的**唯一事实来源**，变更需同步更新。
