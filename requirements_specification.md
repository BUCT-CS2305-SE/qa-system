# 问答子系统（qa-system）需求规格说明书（SRS）

版本：v1.2（结项） | 更新日期：2026-06-10

---

## 1. 引言

### 1.1 背景

本子系统面向 Web 用户提供文物知识问答服务。系统以知识图谱（Neo4j）中的事实数据为主，结合大语言模型（LLM）进行检索增强生成（RAG），在提升可读性的同时优先保证事实准确性，并通过"无数据兜底"和"答案溯源"降低幻觉风险。

知识图谱数据来源于数据组 KG API（`https://se-cs2305.yazs.top`），系统通过 JWT 鉴权方式对接真实 API，支持中/英文双语搜索。

### 1.2 目标

- 提供自然语言问答入口，支持从简单属性查询到复杂关系推理的多层次问答。
- 每条回答必须可溯源，用户可点击跳转到原始数据详情页。
- 当系统缺乏可支撑答案的证据时，必须明确返回"暂无相关数据"。

### 1.3 术语

| 术语 | 定义 |
|------|------|
| KG | 知识图谱（Neo4j），通过数据组 API 访问 |
| RAG | 检索增强生成（Retrieval-Augmented Generation） |
| facts | 从 KG 检索到的结构化事实（subject/predicate/object） |
| sources | 答案来源信息（来源名 + 详情链接） |
| no_data | 没有足够事实/证据支撑回答的状态 |
| hybrid 模式 | KG API 优先，失败自动降级至 mock |
| remote 模式 | 仅调用真实 KG API，失败报错不降级 |
| mock 模式 | 仅使用本地假数据，用于开发/测试 |

---

## 2. 总体描述

### 2.1 系统范围

系统包含三个服务：

| 服务 | 技术栈 | 端口 | 职责 |
|------|--------|------|------|
| Web 前端 | React 19 + TypeScript + Vite 8 | 5173 | 用户交互、对话展示、来源/反馈 |
| 后端网关 | Spring Boot 3.1.4 + Java 17 | 8081 | 鉴权、限流、转发、历史、反馈 |
| RAG 服务 | Python 3.13 + FastAPI | 8000 | 输入理解、KG 检索、答案生成、溯源 |

### 2.2 用户角色

| 角色 | 描述 |
|------|------|
| 普通用户 | 输入问题、查看答案与来源、连续对话、提交反馈 |
| 开发者 | 维护规则/配置、运行测试、部署服务 |
| 管理员 | 查看反馈统计、监控系统状态 |

### 2.3 假设与依赖

| 依赖项 | 说明 |
|--------|------|
| 数据组 KG API | `https://se-cs2305.yazs.top`，需 JWT 鉴权，提供 8 个查询端点 |
| DeepSeek API | LLM 润色（auto 模式），可选配置 |
| H2 数据库 | 后端内嵌，用于历史记录与反馈落库 |

---

## 3. 功能需求

> 标注：P0=必须，P1=重要，P2=可选；✅=已实现，⏳=推迟

### 3.1 问答（P0）✅

| 编号 | 需求描述 | 实现方式 | 状态 |
|------|----------|----------|------|
| FR-001 | 支持用户输入自然语言问题，返回答案与来源 | `POST /api/qa/ask`，统一响应结构 | ✅ |
| FR-002 | 意图识别与实体抽取 | 关键词打分（16 规则）+ 实体别名匹配（22 条，4 类） | ✅ |
| FR-003 | 将识别结果映射为图查询 | 意图 → 16 个查询模板 → 参数填充 | ✅ |
| FR-004 | 从知识图谱检索相关事实 | hybrid 双模：真实 KG API（8 端点）+ mock 降级 | ✅ |
| FR-005 | 根据检索事实组织回答 | rule 模式（16 类模板，~20ms）/ auto 模式（LLM 润色，~3.5s） | ✅ |

### 3.2 简单问答类型覆盖（P0）✅

FR-010：系统覆盖以下 **12 类**简单问答（超出计划的 10 类）：

| 序号 | 意图标签 | 示例问题 | 状态 |
|------|----------|----------|------|
| 1 | `artifact_museum` | "女史箴图在哪个博物馆？" | ✅ |
| 2 | `artifact_period` | "青铜奔马属于哪个朝代？" | ✅ |
| 3 | `artifact_material` | "马踏飞燕是什么材质的？" | ✅ |
| 4 | `artifact_type` | "Tea Bowl and Dish 属于什么类型？" | ✅ |
| 5 | `artifact_description` | "介绍一下清明上河图" | ✅ |
| 6 | `artifact_dimensions` | "女史箴图的尺寸是多少？" | ✅ |
| 7 | `painting_author` | "清明上河图的作者是谁？" | ✅ |
| 8 | `artist_biography` | "顾恺之的生平经历是怎样的？" | ✅ |
| 9 | `same_artist_works` | "张择端还有哪些作品？" | ✅ |
| 10 | `dynasty_representative` | "唐代有哪些代表性文物？" | ✅ |
| 11 | `recommended_artifacts` | "推荐一些和女史箴图类似的文物" | ✅ |
| 12 | `museum_count` | "大都会博物馆共收藏了多少件中国文物？" | ✅ |

> 额外支持裸实体名输入（如"茶碗"→ 自动回退为 artifact_description，"芝加哥博物馆"→ 自动回退为 museum_count）。

### 3.3 无数据兜底（P0）✅

| 编号 | 需求描述 | 实现方式 | 状态 |
|------|----------|----------|------|
| FR-020 | KG/检索证据不足时返回 no_data 状态 | hybrid 模式真实 KG 返回空时不再降级 mock，正确返回 no_data | ✅ |
| FR-021 | no_data 情况下不得生成事实性结论 | answer 固定为"暂无相关数据"，sources 为空 | ✅ |
| FR-022 | 空字段自动拼装 | 描述字段为空时回退为拼装材质/时期/尺寸/收藏地完整介绍 | ✅ |

### 3.4 答案溯源与可信度展示（P0）✅

| 编号 | 需求描述 | 实现方式 | 状态 |
|------|----------|----------|------|
| FR-030 | 每条回答提供 sources | `source_name` + `detail_url`，如 artic.edu 博物馆详情页 | ✅ |
| FR-031 | LLM 补充描述与 KG facts 区分标注 | 前端区分展示"💡 本回答由 deepseek-chat 基于知识图谱事实生成" / "规则版" | ✅ |

### 3.5 多轮对话（P2）✅

| 编号 | 需求描述 | 实现方式 | 状态 |
|------|----------|----------|------|
| FR-040 | 支持上下文用于意图理解 | 30 分钟超时 + 轮次裁剪，前端 localStorage + 后端 H2 双层存储 | ✅ |
| FR-041 | 支持代词指代理解 | 正则匹配"它的材质/尺寸/作者/收藏地" → 回查上一轮实体 | ✅ |
| FR-042 | 支持识别话题切换 | 三重判断：信号词 + 意图变化 + 实体不相关 | ✅ |
| FR-043 | 无上下文时问代词 | 返回 clarify 或 no_data，要求用户明确指定实体 | ✅ |

### 3.6 复杂问答（P2）✅

| 编号 | 需求描述 | 数据组 API | 状态 |
|------|----------|------------|------|
| FR-050 | 多跳推理 | `GET /graph/neighbors/{id}?depth=2` | ✅ |
| FR-051 | 比较类问答 | `POST /artifacts/compare`（双文物 ID 先解析） | ✅ |
| FR-052 | 统计类问答 | `GET /stats/distribution`（四维分布） | ✅ |
| FR-053 | 路径查询 | `GET /graph/neighbors/{id}?depth=2`（子图探索） | ✅ |

### 3.7 反馈机制（P2）✅

| 编号 | 需求描述 | 实现方式 | 状态 |
|------|----------|----------|------|
| FR-060 | 提供"有帮助/不准确"反馈入口 | 前端 👍/👎 按钮 → 后端 `POST /api/qa/feedback` 落库 | ✅ |
| FR-061 | 记录不准确反馈 | H2 持久化，含 request_id、helpful、comment、时间戳 | ✅ |
| FR-062 | 统计高频未命中问题 | RAG 服务 `GET /api/qa/summary`（意图分布/状态分布/失败清单） | ✅ |

### 3.8 安全与治理（P1）✅

| 编号 | 需求描述 | 实现方式 | 状态 |
|------|----------|----------|------|
| FR-080 | API 鉴权 | 双重鉴权：`X-Api-Key` + `Authorization`（JWT 透传至 KG API） | ✅ |
| FR-081 | IP 限流 | 滑动窗口 60s/60 次，超限 HTTP 429 | ✅ |
| FR-082 | 会话管理 | 前端 localStorage（5 天 TTL）+ 后端 H2（30 天）+ RAG 内存（30min） | ✅ |
| FR-083 | 中/英文自适应 | 自动检测语种，分别使用 `lang=zh` / `lang=en` | ✅ |

### 3.9 闲聊与文化导览（P2）⏳

| 编号 | 需求描述 | 状态 |
|------|----------|------|
| FR-070 | 文物文化领域扩展讲解 | 部分（rule 模板提供上下文，LLM auto 模式可生成扩展内容） |
| FR-071 | 超域话题礼貌拒答 | 部分（unknown 意图返回 clarify，但无严格领域边界） |

---

## 4. 接口需求

### 4.1 POST /api/qa/ask（P0）✅

**请求**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `question` | string | 是 | 用户自然语言问题 |
| `session_id` | string | 否 | 会话标识，多轮对话场景必传 |

**响应**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `request_id` | string | 请求唯一标识 |
| `answer` | string | 自然语言答案 |
| `no_data` | boolean | 是否证据不足 |
| `status` | string | ok / no_data / clarify / unknown / exception |
| `intent` | string | 识别到的意图标签 |
| `sources` | array | `[{ source_name, detail_url }]` |
| `facts` | array | `[{ subject, predicate, object }]` |
| `mode` | string | rule / auto |

**约束**：
- `no_data=true` 时 `answer` 必须包含"暂无相关数据"
- `sources` 允许为空

**错误码**：

| 错误码 | 含义 |
|--------|------|
| 2000 | 成功 |
| 4001 | 请求参数无效 |
| 4002 | 系统内部错误 |
| 4003 | KG 服务不可用 |
| 4004 | LLM 服务不可用 |
| 5001 | 鉴权失败（HTTP 401） |
| 5002 | 限流触发（HTTP 429） |
| 5003 | 会话不存在 |
| 5004 | 反馈记录失败 |

### 4.2 POST /api/qa/feedback（P2）✅

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `request_id` | string | 是 | 对应的问答请求 ID |
| `helpful` | boolean | 是 | 有帮助 / 不准确 |
| `comment` | string | 否 | 文字反馈 |

### 4.3 GET /api/qa/history/list（P2）✅

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `session_id` | string | 是 | 会话 ID |
| `page` | int | 否 | 页码（默认 1） |
| `size` | int | 否 | 每页条数（默认 20） |

### 4.4 GET /api/health（P0）✅

- 响应：`{ "status": "ok" }`
- 注意：免鉴权

---

## 5. 非功能需求

### 5.1 性能

| 编号 | 需求 | 目标 | 实测 |
|------|------|------|------|
| NFR-001 | 简单问答（rule 模式，真实 KG） | < 2s | ~1.5s |
| NFR-002 | LLM 生成场景 | 放宽，记录耗时 | ~3.5s（DeepSeek API） |
| NFR-003 | 简单问答（mock 模式） | < 0.5s | ~20ms |

### 5.2 可靠性与可用性

| 编号 | 需求 | 状态 |
|------|------|------|
| NFR-010 | 下游故障可读错误提示与降级 | ✅ hybrid 模式：KG API 不通 → 自动切 mock；remote 模式 → 明确报错 |

### 5.3 安全

| 编号 | 需求 | 状态 |
|------|------|------|
| NFR-020 | 基础鉴权 | ✅ 双重鉴权（X-Api-Key + JWT） |
| NFR-021 | 限流 | ✅ IP 滑动窗口 60s/60 次 |
| NFR-022 | CORS | ✅ 允许 localhost + 局域网 IP |

### 5.4 可观测性

| 编号 | 需求 | 状态 |
|------|------|------|
| NFR-030 | 全链路 request_id | ✅ 记录意图、实体、命中情况、耗时 |
| NFR-031 | 监控指标 | ✅ Micrometer 计数器（QPS、延迟、错误率） |

### 5.5 可测试性

| 编号 | 需求 | 状态 |
|------|------|------|
| NFR-040 | 最小测试题集 + 回归运行 | ✅ 33 条题集 + `run-tests.ps1`，55 用例 100% 通过 |

---

## 6. 数据需求

| 编号 | 需求 | 状态 |
|------|------|------|
| DR-001 | 知识图谱包含文物/作者/朝代/博物馆等实体及属性 | ✅ 数据组 KG API 提供 |
| DR-002 | 每条来源包含可跳转详情页链接 | ✅ artic.edu 等博物馆链接 |
| DR-003 | 实体别名映射 | ✅ `entity_aliases.json`（22 条，中/英双语） |
| DR-004 | 意图规则配置 | ✅ `intent_rules.json`（16 条） |

---

## 7. 验收标准

### 7.1 功能验收

| 验收项 | 标准 | 结果 |
|--------|------|------|
| 覆盖 ≥10 类简单问答 | 可演示 | ✅ 12 类（超计划 2 类） |
| no_data 兜底生效 | 抽测无答案问题不编造 | ✅ 5/5 通过，hybrid + real KG 双重验证 |
| sources 可点击指向详情页 | 前端渲染为可点击链接 | ✅ artic.edu 等博物馆详情链接 |
| `/api/qa/ask` 响应结构稳定 | 前端可直接渲染 | ✅ 统一 status/answer/sources/facts 结构 |
| 回归题集可运行 | 一键运行 + 结果摘要 | ✅ `run-tests.ps1`，55 用例 100% |
| 复杂问答可用 | 4 类可演示 | ✅ 6/6 通过 |
| 多轮对话可用 | 代词指代 + 话题切换 | ✅ 7/7 步通过 |
| 鉴权生效 | 无 Key → 401 | ✅ 5/5 通过 |
| 限流生效 | 超阈值 → 429 | ✅ 并发 65 次触发 429 |
| 健康检查 | 三服务均返回 ok | ✅ 3/3 通过 |

### 7.2 演示脚本（5 场景）

1. **简单问答**（"女史箴图在哪个博物馆？"）→ 溯源链接可点击
2. **多轮对话**（"茶碗在哪里？"→ "它的材质呢？"→ "它的尺寸呢？"）→ 代词继承
3. **复杂问答**（"比较茶碗和青铜奔马"）→ 真实 API 对比
4. **no_data 兜底**（编造问题"xyz123 在哪里？"）→ "暂无相关数据"
5. **反馈按钮**（👍/👎）→ 后端记录 + RAG 统计闭环

---

## 8. 需求实现矩阵

| 需求编号 | 名称 | 优先级 | 状态 | 验证方式 |
|----------|------|--------|------|----------|
| FR-001 | 自然语言问答 | P0 | ✅ | 演示 + 55 测试用例 |
| FR-002 | 意图识别与实体抽取 | P0 | ✅ | 16 意图全覆盖测试 |
| FR-003 | 查询转换 | P0 | ✅ | 16 模板参数填充测试 |
| FR-004 | KG 检索 | P0 | ✅ | 真实 KG API 10 用例 |
| FR-005 | 答案组织 | P0 | ✅ | rule + auto 双模式 |
| FR-010 | 简单问答覆盖 ≥10 类 | P0 | ✅ | 12 类 23 用例 100% |
| FR-020 | no_data 兜底 | P0 | ✅ | 5 用例 100% |
| FR-021 | 不编造事实 | P0 | ✅ | no_data 语义验证 |
| FR-022 | 空字段拼装 | P0 | ✅ | Bug B-007 修复 + 回归 |
| FR-030 | sources 提供 | P0 | ✅ | artic.edu 链接验证 |
| FR-031 | facts/LLM 区分 | P0 | ✅ | 前端标注展示 |
| FR-040~043 | 多轮对话 | P2 | ✅ | 7 步 100% |
| FR-050~053 | 复杂问答 | P2 | ✅ | 6 用例 100% |
| FR-060~062 | 反馈机制 | P2 | ✅ | 1/4 自动化（3 项前端） |
| FR-070~071 | 闲聊导览 | P2 | ⏳ | 部分实现 |
| FR-080~083 | 安全治理 | P1 | ✅ | 鉴权 5 + 限流 1 = 100% |
| NFR-001 | 简单问答 <2s | P1 | ✅ | ~1.5s（真实 KG） |
| NFR-030 | 全链路 request_id | P1 | ✅ | 日志验证 |
| NFR-040 | 回归题集 | P1 | ✅ | 55 用例 100% |

---

## 9. 附录：已知限制

| 限制 | 说明 |
|------|------|
| 文档 RAG 未接入 | 当前仅 KG 单路检索，无向量语义召回 |
| LangChain 未集成 | 当前 RAG 管道为手写实现 |
| 严格领域边界未实现 | 非文物问题可能返回 unknown/clarify 而非固定拒答话术 |
| 性能测试未完整 | 仅 rule 模式实测（~1.5s），未系统压测 |
| 高可用未实现 | 无多副本、熔断、降级面板 |
