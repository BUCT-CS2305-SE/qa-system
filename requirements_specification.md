# 问答子系统（qa-system）需求规格说明书（SRS）

版本：v0.1

## 1. 引言

### 1.1 背景

本子系统面向 Web 用户提供文物知识问答服务。系统以知识图谱（Neo4j）中的事实数据为主，结合（可选）文档检索与大语言模型（LLM）进行检索增强生成（RAG），在提升可读性的同时优先保证事实准确性，并通过“无数据兜底”和“答案溯源”降低幻觉风险。

### 1.2 目标

- 提供自然语言问答入口，支持从简单属性查询到复杂关系推理（选做）的多层次问答。
- 每条回答必须可溯源，用户可点击跳转到原始数据详情页。
- 当系统缺乏可支撑答案的证据时，必须明确返回“暂无相关数据”。

### 1.3 术语

- KG：知识图谱（Neo4j）
- RAG：检索增强生成
- facts：从 KG 检索到的结构化事实
- sources：答案来源信息（来源名 + 详情链接）
- no_data：表示没有足够事实/证据支撑回答的状态

## 2. 总体描述

### 2.1 系统范围

系统包含：
- Web 前端：问答输入、对话展示、来源展示、反馈（选做）。
- 统一问答接口：`POST /api/qa/ask`。
- RAG 服务：输入理解、实体抽取、意图识别、查询构建、KG 检索、内容生成/组织、溯源、反馈记录。

### 2.2 用户角色

- 普通用户：输入问题、查看答案与来源。
- 管理/运营人员（选做）：查看反馈日志、查看未命中/无数据问题统计。
- 开发/测试人员：维护规则、测试集、接口契约与日志。

### 2.3 假设与依赖

- 知识图谱数据可通过 Neo4j 查询获得（Cypher）。
- 文档检索（向量库）与 LLM 接入为可选增强能力。
- 系统对外接口格式需与 `web-frontend` 对齐。

## 3. 功能需求

> 标注：P0=必须，P1=重要，P2=可选

### 3.1 问答（P0）

FR-001：系统应支持用户输入自然语言问题，并返回答案与来源。
- 输入：question（字符串）
- 输出：answer（字符串）、sources（列表）

FR-002：系统应进行意图识别与实体抽取。
- 意图示例：收藏地、年代、材质、类型、介绍、作者、作者生平、同作者作品、同朝代文物、尺寸规格、相似推荐
- 实体示例：文物名、作者名、朝代名、博物馆名等

FR-003：系统应将识别结果映射为图查询语句（优先 Cypher）。

FR-004：系统应从知识图谱中检索相关事实（facts）。

FR-005：系统应根据检索事实组织回答。
- 最小实现：规则/模板化回答
- 增强：LLM 基于 facts/chunks 生成自然语言回答（需受控）

### 3.2 简单问答类型覆盖（P0）

FR-010：系统至少覆盖以下 10 类简单问答（允许实现 11 类或更多）：
1) 文物收藏地
2) 文物年代
3) 文物材质
4) 文物类型
5) 文物介绍
6) 书画作者（书画类）
7) 作者生平（书画类）
8) 同一作者作品（海外收藏）
9) 同一朝代文物
10) 文物尺寸与规格
11) 相关文物推荐（可替代或附加）

### 3.3 无数据兜底（P0）

FR-020：当 KG/检索证据不足以支撑回答时，系统必须返回 no_data 状态，并在 answer 中明确提示“暂无相关数据”。

FR-021：no_data 情况下不得生成看似确定的事实性结论。

### 3.4 答案溯源与可信度展示（P0）

FR-030：每条回答必须提供 sources。
- source_name：来源名称（如“数据来源：克利夫兰艺术博物馆”）
- detail_url/source_url：来源详情页链接

FR-031：若答案中包含 LLM 的补充描述，系统必须与 KG facts 部分明确区分标注。

### 3.5 多轮对话（P2，选做）

FR-040：系统应支持最近 5 轮上下文用于意图理解。
FR-041：系统应支持代词指代理解。
FR-042：系统应支持识别话题切换并重置上下文。

### 3.6 复杂问答（P2，选做）

FR-050：系统可支持多跳推理问答。
FR-051：系统可支持比较类问答。
FR-052：系统可支持统计类问答。
FR-053：系统可支持路径查询。

### 3.7 反馈机制（P2，选做）

FR-060：系统可提供“有帮助/不准确”反馈入口。
FR-061：系统应记录不准确反馈至问题日志，供人工审核。
FR-062：系统可统计高频未命中问题类型，为优化提供依据。

### 3.8 闲聊与文化导览（P2，选做）

FR-070：系统可在文物文化领域内进行扩展讲解与推荐。
FR-071：超出领域话题应礼貌拒答并说明范围。

## 4. 接口需求（对外）

> 说明：此处为“需求级”的接口约束；详细字段契约建议在 `specs/` 进一步固化。

### 4.1 POST /api/qa/ask（P0）

- 请求（最小）：
  - question: string
  - session_id: string（P2，多轮用）

- 响应（最小）：
  - request_id: string
  - answer: string
  - no_data: boolean
  - sources: Array<{ source_name: string; detail_url: string }>
  - facts: Array<{ key: string; value: string; evidence?: string }>

- 约束：
  - no_data=true 时 answer 必须包含“暂无相关数据”字样
  - sources 允许为空，但必须明确标注“无来源/暂无来源”（不推荐）

### 4.2 POST /api/qa/feedback（P2）

- 请求：
  - request_id: string
  - helpful: boolean
  - comment?: string

- 响应：
  - ok: boolean

### 4.3 GET /api/health（P0）

- 响应：
  - status: "ok" | "degraded"

## 5. 非功能需求

### 5.1 性能（P1）

NFR-001：常规简单问答（仅 KG）在本地开发环境下应在 2s 内返回（不含冷启动）。
NFR-002：LLM 生成场景可放宽，但应记录耗时并可配置超时。

### 5.2 可靠性与可用性（P1）

NFR-010：系统应对下游（Neo4j/LLM/向量库）故障给出可读错误提示与降级策略。

### 5.3 安全（P1）

NFR-020：接口应具备基础鉴权/限流能力（可由 Spring Boot 或网关承载）。

### 5.4 可观测性（P1）

NFR-030：每次问答应生成 request_id，并记录意图、实体、命中情况、耗时。

### 5.5 可测试性（P1）

NFR-040：应维护最小测试题集并支持回归运行，输出通过率与失败原因。

## 6. 数据需求

DR-001：知识图谱至少包含：文物、作者、朝代、博物馆等实体及关键属性。
DR-002：每条可展示来源应包含可跳转详情页链接。

## 7. 验收标准

- 覆盖 ≥10 类简单问答并可演示。
- no_data 兜底生效，抽测无答案问题不出现编造。
- sources 可点击并指向详情页。
- `/api/qa/ask` 响应结构稳定，前端可直接渲染。
- 具备最小回归题集与日志定位能力。
